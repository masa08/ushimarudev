import React from 'react';

type Props = {
  className?: string;
};

const PostCardList: React.FC<Props> = (props) => {
  return (
    <div>
      <p>PostCardList</p>
    </div>
  );
};
export default PostCardList;
